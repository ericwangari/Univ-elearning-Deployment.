BASE_URL=https://your-app.vercel.app/
MAIL_FROM=no-reply@your-app.example.com
MAIL_FROM_NAME="Univ E-Learning"
PLATFORM_FEEDBACK_EMAIL=univelearning01@gmail.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-gmail@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_SECURE=tls

# ---- Supabase (PostgreSQL) ----
# Paste your Supabase "Connection String" (URI format) here.
# Find it in: Supabase Dashboard > Project Settings > Database > Connection string > URI
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# ---- MySQL fallback (local XAMPP / Railway) ----
# If DATABASE_URL is set above, these are ignored.
DB_HOST=localhost
DB_PORT=3306
DB_NAME=univ_elearning
DB_USER=root
DB_PASS=
