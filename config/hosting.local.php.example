<?php
/**
 * Local / Shared Hosting Configuration Template
 * Copy this file to `config/hosting.local.php` and fill in your database credentials.
 * Settings in hosting.local.php override environment variables.
 */

return [
    // Database Driver: 'mysql' or 'pgsql'
    'db_driver' => 'mysql',

    // Database Host (e.g. '127.0.0.1', 'localhost', or Supabase pooler host)
    'db_host'   => '127.0.0.1',

    // Database Port (MySQL default: 3306, PostgreSQL / Supabase default: 5432 or 6543)
    'db_port'   => '3306',

    // Database Name
    'db_name'   => 'univ_elearning',

    // Database User
    'db_user'   => 'root',

    // Database Password
    'db_pass'   => '',

    // Base URL of your app (must end with trailing slash)
    'base_url'  => 'http://localhost/univ_elearning/',

    // Public support address shown to users
    'support_email' => 'support@example.com',

    // SMTP email settings for contact support, feedback, and verification emails
    'mail_from' => 'no-reply@example.com',
    'mail_from_name' => 'Univ E-Learning',
    'smtp_host' => 'smtp.gmail.com',
    'smtp_port' => '587',
    'smtp_username' => 'no-reply@example.com',
    'smtp_password' => '',
    'smtp_secure' => 'tls',
    'platform_feedback_email' => 'support@example.com',

    // Google OAuth settings for Sign in with Google
    'google_client_id' => '',
    'google_client_secret' => '',
    'google_redirect_uri' => 'http://localhost/univ_elearning/index.php?page=google-callback',

    // Optional AI teacher settings. Without a key, the app uses built-in study guidance.
    'ai_teacher_api_key' => '',
    'ai_teacher_model' => 'gpt-4o-mini',
];
